# Django Core
Django==5.1.3
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
drf-nested-routers==0.94.1
django-cors-headers==4.6.0

# WebSocket / Channels (for PTY terminal)
channels[daphne]>=4.0.0
daphne>=4.0.0

# Multi-worker ASGI for production / load (gunicorn supervises uvicorn workers).
# NOTE: multi-worker needs a shared channel layer (Redis) for the WS terminal —
# the in-memory layer is per-process. See deploy/LOAD_TESTING.md.
gunicorn>=21.2.0
uvicorn[standard]>=0.30.0

# Windows PTY support (ConPTY via winpty — Linux uses built-in pty module)
pywinpty>=2.0.0; sys_platform == "win32"

# JWT Authentication
PyJWT==2.10.0

# SSH / SFTP (used by workspace/views.py to connect to physical QCar)
paramiko>=3.0.0

# Configuration (.env loading — settings.py calls load_dotenv)
python-dotenv>=1.0.0

# Python utilities
python-dateutil==2.9.0.post0
pytz==2024.2
requests==2.31.0

# Database (SQLite is included with Python)1
# PostgreSQL driver — needed when DATABASE_URL points at Postgres (settings.py
# wires it; see deploy/LOAD_TESTING.md). Harmless when using the SQLite default.
psycopg2-binary>=2.9.9

# Development dependencies (optional)
# pytest==8.3.4 
# pytest-cov==6.0.0
# coverage==7.6.10
